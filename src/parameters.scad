// unit: 1 = 1 mm
eps = 0.01;  // prevent z-fighting
tolerance = 0.4;  // printer's accurarcy
circle_edge = 16;  // approximate a circle with a n-gon

// size of a container box
box_size = 60;
wall_thickness = 2;
floor_gap = 1.0;
hinge_radius = 2.0;


// dimensions of leg
leg_length = 60;
leg_radius = 4;
leg_thickness = 10;
leg_joiner_size = 5;
